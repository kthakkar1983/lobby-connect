# Memory Index — Lobby Connect

- [Project status](memory/project-status.md) — **Plans 1–9 COMPLETE & shipped to prod** (latest tag `plan-09-admin-provisioning-complete`). **Pilot smoke IN PROGRESS:** §1 seed, §2 RBAC, §3 voice, §4 kiosk video, §6 owner, §7 observability, sign-out all PASS. **Session 5 (2026-06-06)** triaged Kumar's bug list: fixed **incoming-voice property name** (TwiML `<Client>` param → "Incoming call · {hotel}"), **softphone token auto-refresh** (1h disconnect; `lib/voice/device-resilience.ts`), and **video-call ringtone** (`public/sounds/ring.mp3` + poll 20s→3s) — all deployed (`f0055fb`, `928eddc`, deploy `dpl_6KeMuCrea…`); owner "double audit" was a **non-bug** (one row per changed field). **Next:** more testing + the only remaining smoke item, **§5 emergency (933-only)**. ⚠️ Go-live still requires `EMERGENCY_DIAL_NUMBER=911` in prod. Read the status file before doing anything.
