# Memory Index — Lobby Connect

> **Sourcing discipline (always):** In every answer and deliverable, clearly separate **source-backed** facts (confirmed by a search/fetch/file/tool — cite it) from **gap-filled** content (inference/estimate/recollection — label it "estimate/unverified/inferred"). Never present an unverified number (price/spec/date) as confirmed; if a search returns nothing, say "could not verify" rather than guessing. Correct earlier gap-fills openly when a source contradicts them. Full rule in `CLAUDE.md` → "Sourcing discipline". (Added 2026-06-19 after unverified scanner-price estimates were off by 3–5x.)

- **CURRENT FOCUS (2026-07-01) — STACK CONSOLIDATION: DESIGNED + LOCKED, migration not started.** Target: one owned **DO 4c/8GB** box (Coolify app hosting + self-hosted **LiveKit** video + self-hosted **RustDesk relay**) + Twilio + **managed Supabase Pro** + Speechmatics ≈ **$84/mo fixed**; Agora + Vercel dropped at migration end. RustDesk integration = **native client + `rustdesk://` deep link from per-property dashboard cards + a persistent always-on-top "deskphone" tile** (Document PiP, open all shift via a "Go on duty" click that primes audio; web-client embed REJECTED: closed-source preview, no auto-connect API, unverifiable in-browser E2EE). **Layered alerting solved product-wide:** tile rings above everything incl. fullscreen YouTube (L1) + **Web Push BUILT in Phase 3, un-demoted** (L2) + Twilio's background-proof audio ring (L3); **Gate 3.0** = 1–2 day tile prototype judged live before Phase-3 proper (fail → thin desktop shell). Dashboard rework: shared property-card component — agent = pod; **admin = pod-grouped fleet view, ring gated by `covering`, Connect on ANY property (locked)**; one-click **hold**; LC brokers/audits hotel-PC credentials; hotel-PC footprint (incl. the AHK + Zebra DS9308 scanner glue) documented. LiveKit-vs-Jitsi rationale recorded (Jitsi = plan B). **Spec: `docs/specs/2026-07-01-stack-consolidation-target-architecture-design.md` · Migration Phases 0–5: `docs/plans/2026-07-01-stack-consolidation-migration.md` · START HERE: `docs/handoffs/2026-07-02-phase1-kickoff-handoff.md`.** Phase 0 DONE 2026-07-01 (merge `d9f04da` — 30-min video-call cap; tag `pre-consolidation-baseline` @ `47b6a49`); **Phase 1 BUILT + SMOKE PASSED 2026-07-02→03 (design→spec→plan→build in one session; region NYC3 — ATL1 lacks the size + the ~95%-India agent workforce anchors US-East, decision record spec §1): SOAK RUNNING, checkpoint ~2026-07-10 — Phase 2 (relay) runs CONCURRENTLY per 2026-07-03 decision.** Box `lc-box-1` NYC3 hardened · Coolify 4.1.2 · staging portal+kiosk live on `staging.`/`staging-kiosk.lobby-connect.com` behind basic auth w/ carve-outs (**kiosk-on-staging gap CLOSED**, e2e walked) · reaper **`*/15`** (backstop proven live) · **nightly prod `pg_dump` + restore drill PASS** (prod data protected at last) · runbook + credentials register in `docs/setup/`. Gotcha recorded: Coolify Traefik labels are verbatim — NO `$$` doubling. Post-soak: stamp DONE, tag `plan-phase1-box-staging-complete`, revoke both `lc-claude` tokens → **Phase 2 (relay)**. Business model (committed earlier same day): agents remote into the hotel PC (PCI firewall), ~5-property pods, manual SOP admin overflow. Prior threads (Web Push alerting, realtime phases 2-4, Vercel Pro) on hold per spec.

- [Project status](memory/project-status.md) — **Plans 1–9 + all UI/UX phases + audit Phases 0–4 COMPLETE. BRAND REVISION layout phase (agent/admin) SHIPPED to prod 2026-06-17: foundation (tokens/fonts/logo) + sign-in split + unified agent/admin shell + sign-in error states + agent/admin DASHBOARDS + shared gradient header (merged `60911a7`, tag `brand-stage5.3-dashboards-complete`; Vercel READY; CI green). Then a 2026-06-17 dashboard-polish pass (session 22 — 5 prod merges `8460a5f`→`45d3a63`): new admin `/admin/phone-health` page (clickable tile); expandable recent-call rows (hotel-name title + note-icon column + notes/details); a persistent Video card under the softphone; video reliability (autoplay ring priming + audio-only camera fallback so a busy webcam no longer drops the call → guest connects); fixed the empty hourly-volume chart. Brand design = `docs/brand/brand-guidelines.md`; impeccable context = `docs/PRODUCT.md` + `docs/DESIGN.md`. Owner portal LAYOUT redesign SHIPPED 2026-06-17 (`c1638cc`) and **kiosk redesign SHIPPED 2026-06-18 (`bd15103`, session 24) + PROD SMOKE CONFIRMED** — the brand-revision LAYOUT phase is now COMPLETE + verified across every surface. Post-smoke fixes (3 merges `443bfc6`/`c62cd47`/`8cf85eb`): lines swapped to the login's `motion` FloatingPaths + slowed; End/Cancel tear down locally then notify server; abortable in-flight call setup; **THE Cancel bug = a full-screen Ringing overlay (`inset-0 z-[1]`, no `pointer-events-none`) covering the controls** so taps never reached Cancel (fixed + `z-20` on CallControls — jsdom can't catch CSS stacking). First-call ring latency = serverless cold-start (Pro keep-warm deferral). **Brand-polish SHIPPED 2026-06-18 (session 25, merge `5c83840` → prod READY): favicon/app-icon (navy tile + reversed mark) + audio in-call overlay redesign (911→header top-right, navy call card w/ hotel local time via the `answered` route, press-Enter-to-save) — audio-only scope, all call/emergency logic byte-identical. The brand revision is now fully complete.** NEXT = the **v1 punch-list** (`docs/v1-punchlist.md`, the v1 tracker — read it first): (A) call reliability — **FIX LOG-CONFIRMED WORKING in prod (session 27, 2026-06-20): audio 4/4 connect, Twilio 10004 alerts → 0, agent now dialed (ON_CALL fix `d18d452`).** STILL OPEN before checking off A: the clean **isolated single-agent smoke** (only Dilnoza reachable — currently Dilnoza+Tejas+Kumar all read online) + **confirm the Twilio concurrency cap in the console** (was 1; raise expected ~06-21). Intermittent "no one available" = Twilio **concurrent-call limit = 1** (raise pending, ~2026-06-21) + routing dialing offline agents at that 1-slot limit; fix = **presence-gate the dial** (new `isReachableForDial` in `lib/voice/presence.ts`; `resolvePrimaryAgent`/`resolveAvailableAdmins` gate on AVAILABLE+fresh; merge `2072105` → prod `dpl_HhkT1Go…`). Confirm the smoke (only your softphone online → calls ring+connect) before picking up B/C/D; (B) UI fixes (Hang up→**blaze** LOCKED, kiosk favicon, hourly chart→3-series audio/video/missed thin bars, "total call duration" body font, desktop type scale, logo+wordmark on all auth pages, prominent incoming property name); (C) finish audio smoke (local-time ticks + Enter-saves-note); (D) plain-English security-posture doc. Owner portal VERIFIED on prod 2026-06-18. Shipped seams: agent dashboard is agent-scoped not pod-wide (RLS `calls_select`); phone-health "path down" red + per-call real failure-reason are v2 seams; admin off-tab video nudge deferred (`docs/v2-backlog.md`). Prod emergency = 911 (safe).** **Session 27 (2026-06-20) also shipped:** Sentry hygiene (PRs #20/#21 — gate dev events, Twilio transport-noise filter, `pnpm sentry:issues` CLI + read-scoped `SENTRY_READ_TOKEN`, `/admin/status` card fixed: read token + `lastSeen:-24h` + 120s cache; new memory `sentry-observability-access`) + presence-display mismatch fix (PR #22 — `/admin/users` now uses `effectivePresence` like the dashboard/routing). **Session 28 (2026-06-20):** §B + §C **confirmed done** (Kumar); §D **done** — `docs/security-posture.md` (audit-style: auth/tokens/caching/PII/service-role) + a `security-review` rigor pass + a live Supabase security-advisor check (**no high/med exploitable vuln**; 2 defense-in-depth items filed to `docs/v2-backlog.md` — kiosk-token revocation + Agora-uid namespace) + OWNER presence → "—" in `/admin/users`; desktop type scale raised 106.25%→**112.5%** (next rungs 118.75%/125%, one-line knob in `apps/portal/app/globals.css`). PRs **#23 + #24 merged to `main` → prod**. **v1 now down to §A only** (isolated single-agent voice smoke + confirm the Twilio concurrency cap in console; + the lower-priority real-Device-registration hardening). Read the status file before doing anything. **Session 29 (2026-06-21):** **v1.0.0 cut + [GitHub Release](https://github.com/kthakkar1983/lobby-connect/releases/tag/v1.0.0)** (tag on `cf81e48`; package.jsons→1.0.0) + **versioning policy** (`docs/VERSIONING.md`, semver via tags+Releases, PR-per-release from v1.1) + **staging environment built** ($0, persistent): Supabase `lobby-connect-staging` (ref `cgtvqjxhbojztzumshca`, migrated 0001–0017) + a `staging` git branch → Vercel Preview deploys (env scoped Preview/`staging`; Twilio/Agora/Sentry shadowed off; portal `…-portal-git-staging-kumar-thakkars-projects.vercel.app`). **Portal sign-in verified by Kumar; prod-isolation proven.** Runbook: `docs/setup/2026-06-21-staging-runbook.md`. v1 §A unchanged.
