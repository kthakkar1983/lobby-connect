# Memory Index — Lobby Connect

- [Project status](memory/project-status.md) — **Plans 1–9 COMPLETE & shipped to prod** (latest tag `plan-09-admin-provisioning-complete` — email-free admin provisioning). **Pilot smoke IN PROGRESS (2026-06-05):** §1 seed, §2 RBAC, §4 kiosk video pass; Plan 9 stuck-user recovery validated in prod. This session fixed **voice** (property `routing_did` was `+14058610196` ≠ the account's only Twilio # `+14058750410` → corrected on prod; the 11200/protected-URL was a red herring, Twilio already points at the clean alias) and **sign-out** (Radix `DropdownMenuItem`+nested `<form>` swallowed the submit → `d52f6be`, deployed `dpl_A5dnvjx2…`). **Next:** retest §3 voice connect + sign-out, then §5 emergency (933-only), §6 owner, §7 observability. ⚠️ Go-live still requires `EMERGENCY_DIAL_NUMBER=911` in prod. Read the status file before doing anything.
