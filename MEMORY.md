# Memory Index — Lobby Connect

- [Project status](memory/project-status.md) — **Plans 1–9 + all UI/UX phases + audit Phases 0–4 COMPLETE. BRAND REVISION layout phase SHIPPED to prod 2026-06-17: foundation (tokens/fonts/logo) + sign-in split + unified agent/admin shell + sign-in error states + agent/admin DASHBOARDS + shared gradient header — all merged to `main` (`60911a7`, tag `brand-stage5.3-dashboards-complete`) and deployed (Vercel READY; CI green). Brand design = `docs/brand/brand-guidelines.md`; impeccable context = `docs/PRODUCT.md` + `docs/DESIGN.md`. NEXT = owner portal LAYOUT redesign (fresh chat — owner inherits the shared gradient header, keeps its own `UserMenu`, mobile-first), then kiosk LAYOUT + audio in-call. Owner/kiosk currently have the new brand COLORS but OLD layouts. Two shipped seams to remember: agent dashboard is agent-scoped not pod-wide (RLS `calls_select`); phone-health "path down" red is a v2 seam (twilio_webhook info-mode). Prod emergency = 911 (safe).** Read the status file before doing anything.
