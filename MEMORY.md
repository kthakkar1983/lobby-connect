# Memory Index — Lobby Connect

- [Project status](memory/project-status.md) — **Plans 1–9 COMPLETE & shipped. All four UI/UX phases COMPLETE (Stage 1 foundation + 3 Stage 2 repaints + Stage 3 states/motion/a11y/copy), merged to `main` + prod.** **Session 13 (2026-06-08):** prod `EMERGENCY_DIAL_NUMBER` **REVERTED 933→`911`** (verified via decrypted env reads + redeploy; TEMP-emergency-933 memory deleted) — **prod emergency dialing is now real 911, safe for pilot.** Also **FIXED the kiosk video ~120s mid-call disconnect** (ring timer wasn't cleared on connect → fired mid-call, stranded kiosk on Connected screen) — commit `f26488a`, kiosk deployed, **confirmed by a clean 4-minute prod call.** Kumar reports **every v1 feature working from testing**; the **§5 emergency 933 smoke is DONE/cleared**. **Session 14 (2026-06-09): crossed-W font fix DONE** — swapped **Solitude→Atelier** (display) + **Vonique 43→Radon** (labels — which ALSO had the crossed-W) across both apps; token-layer only (no component edits); typecheck/builds/runtime font-load verified; committed straight to `main` → prod. **NEXT (fresh chat): the page-by-page final-polish pass** (kiosk subtle shadows/bounce + live in-browser visual/a11y pass) — deferred this session, not started. Read the status file before doing anything.
