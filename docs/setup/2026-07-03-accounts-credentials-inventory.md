# Accounts & credentials inventory — stack consolidation (as of Phase 1, 2026-07-03)

**No secrets live in this file — ever.** This is the register of every account/credential the consolidation has created or touched: what it is, its identifier, **where the secret actually lives**, and its lifecycle. Kumar's password manager (PM) is the source of truth for human passwords; Coolify env vars hold machine secrets. Companion: `docs/setup/2026-07-02-box-ops-runbook.md` §2 (access) — this doc is the full register, that one is the operational quick-ref.

## 1. Accounts (human logins)

| Account | Identifier | Secret lives in | 2FA | Unlocks |
|---|---|---|---|---|
| DigitalOcean | kthakkar.1983@gmail.com | PM | ON (per setup) | droplet `lc-box-1`, firewall, alert policies, billing |
| Cloudflare | Kumar's account | PM | ON (per setup) | `lobby-connect.com` registrar + DNS zone (later: R2 backups) |
| Coolify (self-hosted) | admin account Kumar registered at https://coolify.lobby-connect.com | PM | ON | all deploys, env vars, scheduled tasks on the box |
| Supabase | Kumar's account | PM | — | staging project `cgtvqjxhbojztzumshca` + prod project `ztunzdpmazwwwkxcpyfp` dashboards |
| GitHub | kthakkar1983 | PM | — | repo + the `lc-coolify` GitHub App settings |
| **Staging basic auth** (browser popup on both staging hosts) | user `staging` | PM entry "LC staging basic-auth" | n/a | viewing https://staging.lobby-connect.com + https://staging-kiosk.lobby-connect.com (bcrypt hash lives in Traefik labels on both Coolify apps — rotation how-to: runbook §10) |
| **Staging portal admin** (app sign-in) | `admin@staging.lobbyconnect.local` | PM entry "LC staging admin" (set by Kumar at forced first-login change 2026-07-03; the old lost password was reset via GoTrue admin API) | app-level | staging portal ADMIN role |

## 2. Machine credentials (keys, tokens, roles)

| Credential | Identifier / location | Secret lives in | Purpose | Lifecycle |
|---|---|---|---|---|
| SSH keypair for the box | `~/.ssh/lc_box` (+ `.pub`) on Kumar's Mac; registered in DO as `lc-box-key` (ID 57536116) | the private key file itself — never leaves the Mac; no passphrase | `root@159.203.124.112` (only from Kumar's static IP 70.184.31.21) | permanent; if the Mac is lost: DO console → droplet → Access → reset via Recovery Console, add a new key |
| DO API token `lc-claude` | DO console → API | pasted in chat 2026-07-02 (Kumar's call), stored in `~/.config/doctl/config.yaml` | provisioning + debugging via `doctl` | **kept through the migration for debugging (Kumar, 2026-07-03) — REVOKE at migration end (Phase-5 close)** |
| Coolify API token `lc-claude` | Coolify → Keys & Tokens | pasted in chat 2026-07-03 | app/env/task management + log pulls via API | **kept through the migration for debugging (Kumar, 2026-07-03) — REVOKE at migration end (Phase-5 close)** |
| Coolify GitHub App `lc-coolify` | app ID 4204806, installation 144083654, scoped to `kthakkar1983/lobby-connect` only | private key held inside Coolify (`coolify-db`) | auto-deploy webhooks + repo pulls | permanent; rotate via GitHub App settings if ever compromised |
| Coolify instance secrets | `/data/coolify/source/.env` on the box | PM (copied 2026-07-03, installer's advice) | Coolify's own encryption keys — needed to rebuild/restore Coolify itself | re-copy to PM after any Coolify major upgrade |
| Staging Supabase `service_role` key | Supabase dashboard → project `cgtvqjxhbojztzumshca` → Settings → API Keys → Legacy | Coolify → `lc-portal-staging` env `SUPABASE_SERVICE_ROLE_KEY` (pasted by Kumar in UI) | portal server-side admin operations on staging | rotate in Supabase dashboard if leaked → update Coolify env → restart app |
| Staging `CRON_SECRET` | — | Coolify → `lc-portal-staging` + `lc-ops` env (same value; generated `openssl rand -hex 32` by Kumar) | authenticates the ops container's cron curls | rotate: new value in BOTH apps → restart both |
| Staging `KIOSK_CONFIG_SECRET` | — | Coolify → `lc-portal-staging` env (generated by Kumar) | signs kiosk `?t=` links for staging | rotating invalidates existing staging kiosk links (regenerate from property detail) |
| Prod DB role `lc_backup` | Postgres role on prod (`ztunzdpmazwwwkxcpyfp`); `LOGIN` + `pg_read_all_data` + `BYPASSRLS`; created 2026-07-03 via dashboard SQL editor | password: PM + inside `PROD_DB_URL` in Coolify → `lc-ops` env | nightly read-only `pg_dump` of prod | rotate: `alter role lc_backup password '…'` in prod SQL editor → update `PROD_DB_URL` → restart `lc-ops`. **Never the master password in scripts.** |
| **RustDesk relay server keypair** (Phase 2, 2026-07-03) | `id_ed25519` + `.pub` in `/opt/rustdesk/data/` on the box (generated by hbbs on first start) | **private key: PM secure note "LC relay server keypair" + Mac `~/.ssh/lc_relay_id_ed25519`** (0600). The **public** key (fingerprint `…3GY=`) is pinned in every client config — not cryptographically secret, but under `-k _` it acts as the **relay access token**, so the full string also stays out of this public repo (sources: box · Mac `.pub` · PM) | server identity: clients pin the public key; `-k _` rejects clients without it; E2EE session verification | **top of the never-lose list** — losing the private key forces re-pinning every client (rotation how-to: runbook §12). NEVER in the repo |

## 3. URLs (what's where)

| Surface | URL | Guarded by |
|---|---|---|
| Coolify console | https://coolify.lobby-connect.com | Coolify admin login + 2FA |
| Staging portal | https://staging.lobby-connect.com | basic auth → app sign-in |
| Staging kiosk | https://staging-kiosk.lobby-connect.com | basic auth (kiosk itself needs a signed `?t=` link) |
| Box SSH | root@159.203.124.112 (IPv6 2604:a880:800:14:0:3:316e:3000) | key-only + DO firewall (22 restricted to Kumar's static IP) |
| Prod portal/kiosk | still on Vercel (unchanged until Phase 5) | — |

## 4. Revocation checklist — at migration end (Phase-5 close; timing per Kumar 2026-07-03)

- [ ] DO API token `lc-claude` — revoke in DO console → API
- [ ] Coolify API token `lc-claude` — revoke in Coolify → Keys & Tokens
- [ ] Confirm PM has all 7 "PM" rows from §1 + the `lc_backup` password + the Coolify `.env` copy *(do this one now, not at migration end)*

## 5. PM entry checklist (mirror this in the password manager)

1. DigitalOcean login · 2. Cloudflare login · 3. Coolify admin (+ note: 2FA recovery codes) · 4. Supabase login · 5. GitHub login · 6. **LC staging basic-auth** (user `staging`) · 7. **LC staging admin** (`admin@staging.lobbyconnect.local`) · 8. **lc_backup prod DB password** · 9. **Coolify instance .env** (secure note) · 10. GitHub App recovery = via GitHub account (no separate secret) · 11. **LC relay server keypair** (secure note: contents of `~/.ssh/lc_relay_id_ed25519` + `.pub` — Phase-2 runsheet H2) · 12. **RustDesk unattended — <property>** (one per hotel PC as provisioned; pilot's at cutover H5) · 13. **RustDesk exported server config** (string from runsheet H3; pasted into LOCAL provisioning-script copies only — encodes the relay key)
