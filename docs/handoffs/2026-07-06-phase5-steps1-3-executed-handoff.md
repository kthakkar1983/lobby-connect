# Handoff — Phase-5 steps 1-3 EXECUTED (Vercel frozen · PR #29 merged · Agora stripped) → next = Phase 3 D (call tile) — START HERE

**Written:** 2026-07-06, end of the blue-green execution session (same day as the decision session). **Supersedes:** `2026-07-06-bluegreen-cutover-decision-handoff.md` (its "NEXT SESSION, in order" is fully executed; its DECISION section + rollback-guarantee framing still stand — read that section if the blue-green model is unfamiliar). **Branch state at close:** `main` = `25a276a` (LiveKit-only trunk) · `staging` = synced to `main` (box serves it) · working branches merged + kept on origin (`agora-strip`, `phase3-workspace`).

## What happened this session (all 2026-07-06)

1. **Vercel FROZEN (Phase-5 step 1).** Executed as the strictest variant: `vercel git disconnect` on BOTH projects (Kumar-approved via ask; the plan's `vercel.json` option was ruled out — it only takes effect via a push to `main`, a chicken-and-egg). Verified three ways: probe push to a branch that had been building previews 4s after every push → zero deployments; then silent on the PR #29 merge push; then silent on the PR #30 merge push. **Standby of record: portal `dpl_7PQ1P7Ui41UD8wrpZrV3FZ2koj6y` + kiosk `dpl_FxZhsJQVLEUn5V2M81gBwvKch5Mu`, both `main@f4af480`, aliased to the two pilot URLs.** Domains/envs/crons untouched — the pilot serves exactly as before. Reversal if ever needed: `vercel git connect` per project (needs the GitHub app, still installed).
2. **PR #29 merged (`bc602ce`)** — Phase 3C + Phase 4 landed on `main`. Deployed nothing (that's the point).
3. **Agora STRIPPED (Phase-5 step 3).** Branch `agora-strip`, five commits, **PR #30 merged → `main` = `25a276a`**:
   - `b2d5e28` TEMP guest-audio diagnostics removed (2026-06-30 list §4; pure deletion)
   - `40b146d` kiosk LiveKit-only (legacy module + adapter + SDK dep gone)
   - `ccc2c7a` portal LiveKit-only (video-call agora branch, `lib/agora/`, `/api/agora/token` + CORS line, both deps, `AGORA_*`/`VIDEO_PROVIDER` env templates)
   - `8f81374` shared fold (`AgoraTokenResult` deleted; `VideoTokenResult` = livekit shape; the Task-2/3 narrowing guards retired; protocol.ts prose reworded, values byte-identical; CLAUDE.md/README truth pass)
   - `c048849` final-review fold-ins (the ONE lost test restored — kiosk wrong-property 403 on the token route — + wire-param comments + doc nits)
   - Discipline: subagent-driven, fresh implementer + spec review + quality review per task (all four quality reviews caught something real: a write-only ref, a stale describe title, an env-header gap, README standby-drift), final whole-branch review = SHIP. **740 tests · LiveKit test files zero cumulative diff · LiveKit sources comment-only · `agora_channel_name` byte-identical everywhere.**
4. **Staging re-smoke on the stripped build (Kumar): PASS** — video connect/complete, agent-side End, cancel-while-ringing (immediate re-call OK), Photo Booth camera-concurrency bonus finding (macOS shares the camera; the busy-cam fallback is for exclusive-lock platforms, i.e. the Windows hotel-PC world).
5. **Captions fixed on staging — it was never the code.** Kumar's correction ("captions didn't work yesterday either") reframed it; source-backed root cause: **the Phase-1 staging env list never included `SPEECHMATICS_API_KEY`** (not even as a deliberate empty). Key recovered from `apps/portal/.env.local` (gitignored, never tracked) → now in **PM + Coolify staging + a new credentials-register row**. **Captions verified live on staging 2026-07-06.** Migration-plan step 5 amended: box-prod gets the FULL Vercel env copied checklist-style, not rebuilt from memory.
6. **Video-quality question answered with evidence.** Kumar's webrtc-internals dump: publisher sends all 3 simulcast layers (180/360/720), **receiver gets the full 720p layer**, transport is **UDP via srflx** (no TCP fallback, no relay). So the "doesn't feel 720p" softness = the **defaults**: 720p capture cap + VP8 software encode at default bitrate on a big cover-scaled stage — both adapters pass ZERO options (`new Room()`, `createLocalVideoTrack()`). → **Pre-cutover tuning spike recorded in migration-plan step 5** (explicit 1080p/720p presets, raised bitrate, evaluate H.264 hardware encode; camera hardware is the ceiling). Dilnoza's night-1 India calls remain the real-world quality gate.

## NEXT SESSION: Phase 3 D — the call-scoped tile (plan Tasks 16-17)

- Spec: `docs/specs/2026-07-04-phase3-workspace-design.md` (D1-D12) · plan: `docs/plans/2026-07-04-phase3-workspace.md`. Tile = Document PiP, opens on Answer, guest-video-first (kiosk eye contact over RustDesk), dies at hang-up. Gate-3.0 prototype learnings apply (PiP chrome ambiguity → reopen-nudge; the prototype page `/duty-tile-prototype` still exists for reference).
- **Open the session with the duty-state design cluster (found in this session's re-smoke):** `onDuty` inits `true` on every softphone mount (`apps/portal/components/softphone/softphone.tsx:96`) → hard refresh/login = on-duty + heartbeating with no button press; End-shift doesn't survive a refresh (client state only). Fold in the already-open design confirm (End-shift silences audio dialing but not video push targeting). Direction to brainstorm with Kumar: **duty as a server-persisted state that alone gates heartbeat + audio dial-targeting + video push-targeting + the fleet label, entered only via "Go on duty"** — decide persistence (profiles column vs presence-derived), refresh semantics, and admin visibility. This is a design conversation first, not a straight build.
- ⚠ DEP-HYGIENE remains the standing risk for tile work (two prior render-loop OOMs): effects depend on stable dispatchers, hooks returning arrays that feed consumer effects MUST memoize.

## State of every moving part

- **Prod pilot:** frozen Vercel standby (Agora), serving normally. Untouchable by any push. Its crons (presence 08:00 + reaper 20:00 UTC) still run against prod Supabase — fine and intended while it IS prod.
- **`main`:** LiveKit-only trunk. Merging PRs deploys nothing anywhere until Phase-5 step 5 stands up box-prod apps.
- **Staging (box):** serves `staging` branch = `main` + captions env fixed. The only pre-prod proof surface.
- **Phase-5 remaining:** steps 4-10 — Phase 3 D/E on the trunk → box prod apps (FULL env copy incl. `SPEECHMATICS_API_KEY`; 0019/0020 to prod Supabase then) → custom domains pointed at Vercel first → runsheet + rollback rehearsal → GO LIVE (Dilnoza night-1) → ~2-week standby window → decommission (Vercel projects deleted, Agora account closed, `@vercel/analytics` removed, tokens revoked, tags `plan-phase4-livekit-complete` + `plan-phase5-cutover-complete`, DO backups + Supabase Pro).
- **Carry-forwards:** Phase-1 soak checkpoint **~2026-07-10** → stamp + tag `plan-phase1-box-staging-complete` · Phase 2 waits on **Dilnoza's clean real night** → stamp + tag `plan-phase2-relay-complete` · GitHub secret-scanning alert still open · pilot phone line not transferred · dashboard/softphone-tile layout rework post-migration · v2-backlog: mid-call resume + ChunkLoadError reload-guard (design together) · post-cutover cleanups: Coolify staging's unread `VIDEO_PROVIDER` env var, the Traefik `/api/agora/*` carve-out label (route now 404s), LiveKit test-mock dedup (was byte-frozen during the strip) + `video-call-livekit.test.tsx`'s stale "provider branch" header comment.

## Gotchas / discipline (carried + new)

1. **Standby invariants:** frozen deploys (verified, but stay paranoid) · ADDITIVE-ONLY migrations · `agora_channel_name` unrenamed · Vercel `AGORA_*` envs + Agora account stay until decommission.
2. **Env provisioning by checklist, not memory** — the captions gap existed for 4 days across two smokes because the Phase-1 env table was built from the visible-failure set. At cutover, diff the box-prod env list against `vercel env ls` for BOTH projects before go-live.
3. **Review-subagent tree pollution (new):** review agents comparing base versions can dump `git show`d files into the real working tree as untracked resurrections of deleted paths — the commits were clean but the local gate broke. `git status --porcelain` before every gate run; clean with targeted `rm`, never blanket `git clean` (pre-existing untracked dirs like `docs/wireframes/`, `supabase/functions/` are not yours).
4. Claude merges PRs on Kumar's explicit go ("ready" counts); docs-only wrap PRs still go through Kumar's click. Pushing `staging`/feature branches is allowed. Commit trailer this session: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
5. Two-stage reviews + byte-gates + final whole-branch review: 4-for-4 on catching real defects this session. Keep the pattern for Phase D.

## Register reminder

Real dialogue, plain English; decide when one answer is sane (the freeze method), converse on genuine forks (none tonight — the decision session settled them). Evidence before fixes (captions and quality both resolved by evidence, not guesses). Sourcing discipline: mark source-backed vs gap-filled — the LiveKit-defaults claims were flagged as prior-knowledge until webrtc-internals confirmed them. The standby is the safety net; protect its invariants.
