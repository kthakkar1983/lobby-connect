# CLAUDE.md — Lobby Connect

Auto-loaded by Claude Code at session start. Read this first, then check `MEMORY.md` and `docs/specs/` for context as needed.

## What this is

**Lobby Connect** — after-hours outsourced **virtual front-desk agents** for hotels that can't staff night shifts. Phone routing + tablet video is only the *guest-facing half*; the other half is that agents **remote into the hotel's own PC (RustDesk) to do the real front-desk work** — check-ins, creating/modifying reservations, night audit. Remote-desktop is a **deliberate PCI firewall**: all card/PII/PMS work stays on the hotel PC, so Lobby Connect **never touches cardholder data and stays out of PCI-DSS scope**. Do NOT design toward pulling payments/PMS into Lobby Connect — that breaks the firewall the model is built on. One agent **owns** a **dedicated pod of ~5 properties** — same faces week to week (virtual *employees*, not an anonymous call center) — with a small admin bench as **manual, SOP-coordinated** overflow (admins flip a `covering` toggle by hand; **no auto-widening**). At launch, Twilio concurrency is raised so the already-built parallel dial (primary agent + covering admins) can place multiple legs; if all eligible humans are busy, the extra call gets the apology (no queue/hold in v1). The agent's *foreground* app is the remote session; the portal sits in the *background* → **OS-level call alerting is mandatory** (an in-tab ring is invisible). Solo build (Kumar + Claude). v1 = pilot one hotel end-to-end. See `docs/PRODUCT.md` "Operating model" for the full framing.

**Current focus (2026-07-02, updated):** the **stack consolidation is DESIGNED + LOCKED.** Target: one owned DO 4c/8GB box (Coolify + self-hosted LiveKit video + self-hosted RustDesk relay) + Twilio + managed Supabase (Pro) + Speechmatics, ~$84/mo fixed; Agora and Vercel are dropped at the end of the migration. RustDesk integration = **native client + `rustdesk://` deep link from per-property dashboard cards + a persistent always-on-top "deskphone" tile** (Document PiP, opened all shift by a "Go on duty" click; the web-client embed was researched and REJECTED — closed-source preview, no auto-connect API, unverifiable in-browser E2EE). Alerting is layered and solved product-wide: the tile rings above everything incl. fullscreen YouTube + **Web Push OS notifications (BUILT in Phase 3, no longer a parked seam)** + Twilio's already-background-proof audio ring; Phase 3 opens with a 1–2 day tile **prototype gate** (fallback = thin desktop shell). Admin dashboard = same card system, **pod-grouped fleet view**, ring gated by `covering`, **admins Connect to any property**. **Spec: `docs/specs/2026-07-01-stack-consolidation-target-architecture-design.md` · Migration (Phases 0–5): `docs/plans/2026-07-01-stack-consolidation-migration.md` · Session handoff (START HERE): `docs/handoffs/2026-07-02-phase1-kickoff-handoff.md`.** Phase 0 DONE 2026-07-01 (merge `d9f04da` = 30-min video-call cap; tag `pre-consolidation-baseline`). **Phase 1 design DONE + spec WRITTEN 2026-07-02** — inputs collected: **`lobby-connect.com`** purchased on Cloudflare Registrar; DO account + full-access API token in hand (`doctl` authed on the Mac; revoke/rotate post-build). Region resolved **NYC3** (ATL1 doesn't offer the 4c/8GB size, `doctl`-verified; and the newly-surfaced **~95%-India agent workforce** anchors US-East — DB/Twilio/hotels are the immovable US anchors, and the India↔US leg is fixed geography no middle hop can shorten; decision record in the Phase-1 spec §1). Design: Dockerfiles not Nixpacks (env-gated `output:"standalone"`), Traefik basic-auth w/ carve-outs (`/api/kiosk/*` + `/api/agora/*` + `/api/cron/*` — each self-secured), Coolify ops-container crons (reaper finally `*/15` on staging), **nightly prod `pg_dump` + restore drill** (free tier has zero backups), DO auto-backups + Supabase Pro still deferred to Phase 5 (~$49/mo net-new during migration). **Phase 1 BUILT + SMOKE PASSED 2026-07-02→03 (spec→plan→build in one session): SOAK RUNNING** — done-when checkpoint = a week unattended (~2026-07-10); **Phase 2 (RustDesk relay) runs CONCURRENTLY with the soak** (decision 2026-07-03 — the migration plan's own "independent, do it early"; pre-phase2 snapshot first; pilot-PC repoint LAST). Staging now serves from the box: `staging.`/`staging-kiosk.lobby-connect.com` behind basic auth with kiosk/agora/cron carve-outs (**the kiosk-on-staging 401 gap is CLOSED** — e2e walked), reaper finally **`*/15`** (backstop proven live on a leaked RINGING row), **nightly prod `pg_dump` + restore drill PASS** (prod pilot data protected for the first time). Plan: `docs/plans/2026-07-02-phase1-box-domain-staging.md` · **runbook: `docs/setup/2026-07-02-box-ops-runbook.md` + credentials register: `docs/setup/2026-07-03-accounts-credentials-inventory.md`**. Gotcha: Coolify passes Traefik labels verbatim — NO compose-style `$$` doubling. Post-soak: stamp DONE in the migration plan + tag `plan-phase1-box-staging-complete` (both `lc-claude` API tokens stay for debugging until migration end — Phase-5 close — per Kumar 2026-07-03). **Next-session START HERE: `docs/handoffs/2026-07-03-phase2-relay-kickoff-handoff.md`** (Phase-2 relay runsheet, with the July-10 soak checkpoint folded in). Prior threads (Web Push alerting, realtime phases 2-4, Vercel Pro) remain on hold per the spec. The Stack list below is the *current* (pre-consolidation) setup.

## Stack

- **Portal** (`apps/portal/`): Next.js App Router on Vercel — agent + admin + owner dashboards, Twilio webhook routes, API routes for Agora tokens
- **Kiosk** (`apps/kiosk/`): Vite SPA on Vercel — tablet-locked, Agora client, no auth
- **Database/auth**: Supabase (Postgres + Auth + Storage)
- **Voice**: Twilio
- **Video**: Agora
- **Remote desktop** (agent → hotel PC): RustDesk. Pilot uses the public/free relay; target = **self-hosted relay on a VPS** (no public servers — speed + security). Criteria = free/OSS + self-hostable. Treated as a separate program today; integration approach now **LOCKED** (2026-07-01): native client launched via `rustdesk://connection/new/<id>?password=<pw>` deep links from per-property dashboard cards, credentials brokered/audited by LC, rings + call controls live in a persistent always-on-top deskphone tile (Document PiP) — see the stack-consolidation spec. A first-class stack pillar, not incidental. The hotel-PC footprint also includes the AHK + Zebra DS9308 license-scanner glue (separate install, SynXis-specific; spec §11).
- **Errors**: Sentry
- **Icons**: lucide-react
- **UI library**: shadcn (light mode only)
- **Styling**: Tailwind + CSS custom properties

## Where to find stuff

| File | What it owns |
|---|---|
| `docs/specs/2026-05-27-v1-architecture-design.md` | The full design spec — locked decisions, schema, routing, UI/UX baseline |
| `docs/plans/` | Implementation plans, one per major build phase |
| `docs/decisions/` | ADRs for individual locked decisions (created on demand) |
| `MEMORY.md` | Index of session-specific memories |
| `supabase/migrations/` | All SQL migrations, in order |

## v1 scope (cheat sheet)

**Ship:** phone routing, agent dashboard, admin CRUD (agents/properties/staff), owner portal (mobile responsive), kiosk video (Agora), audit log, status page.

**Cut from v1, schema-ready for later:** voicemail, voicemail callback queue, PagerDuty, ops dashboard, held-call slot, backup agent ringing, MFA, audio transcription, magic link sign-in, mobile-responsive agent/admin portals, dark mode.

## Locked decisions

1. **Routing**: parallel dial to primary agent + admins with `accepting_calls=true`, 120s window, apology TwiML on timeout. No voicemail.
2. **Roles**: AGENT, ADMIN, OWNER. Stored as text + CHECK constraint, not Postgres enum.
3. **Auth**: Supabase Auth, password only, invite-only, RLS on every table, Next.js middleware gate.
4. **Realtime**: 20s polling + refetch-on-focus + optimistic mutations. No subscriptions.
5. **UI**: light mode only. Collapsed icon-sidebar with hover-expand. Logo = home. Skeleton 10s timeout. Mobile-responsive owner portal only.
6. **Tenancy**: every table has `operator_id`. v1 is single-tenant. v2 query-layer filter unlocks multi-tenant SaaS.
7. **Forward-compat**: all cut features have schema/code-structure hooks. No destructive migrations needed to re-enable.

## Conventions

- **Never hardcode hex colors** — use Tailwind tokens (`bg-primary`, `text-text-muted`, `border-border`)
- **Never bypass RLS** in app code — use service role only in API routes that genuinely need it (Twilio webhooks, cron jobs, admin invitations)
- **All migrations** go in `supabase/migrations/` and are committed before being applied
- **Audit log** every meaningful change (create/update/delete on top-level entities, role changes, sign-in/out)
- **No emojis in code or commits** unless explicitly requested
- **Spec changes**: edit `docs/specs/*.md` files; old versions are in git history, not "v2 file"
- **Versioning**: semver (`vMAJOR.MINOR.PATCH`) via git tags + GitHub Releases; bump the four `package.json`s at release. Policy: `docs/VERSIONING.md`. (`plan-*` tags are a separate milestone axis.)

## Sourcing discipline (ALWAYS — applies to all research, claims, numbers, and reports)

- **Mark every factual claim as one of two things: source-backed or gap-filled.** Never blur the line.
- **Source-backed** = directly confirmed by a search result, fetched page, file, or tool output. Cite it (link or filename). Prices, specs, dates, names, availability, and "who/what/when" facts MUST be source-backed.
- **Gap-filled** = inference, estimate, recollection, or reasoning not tied to a source. Label it explicitly in-line: "estimate", "unverified", "from prior knowledge — not re-checked", "inferred", etc.
- **Never present an unverified number (price, spec, date) as if it were confirmed.** If a search returns no figure, say "could not verify" — do NOT substitute a plausible guess. (Lesson: M280/M500/QS2000 price estimates given without verification were wrong by ~3–5x.)
- **When a prior gap-filled claim is later contradicted by a source, correct it openly** and note what changed.
- Applies to chat answers AND any saved deliverable (reports, docs). In documents, keep a visible confidence/sourcing note on uncertain items.

## Deployed URLs (v1, Vercel-provided, custom domain deferred)

- Portal: `lobby-connect-portal.vercel.app`
- Kiosk: `lobby-connect-kiosk.vercel.app`

## Build status

| Plan | What it built | Tag |
|---|---|---|
| 1 — Foundation | Shell, monorepo, lint, typecheck, Vitest | `plan-01-foundation-complete` |
| 2 — Database & RLS | `0001_init.sql`, `0002_rls.sql`, `seed.sql`, TS types | `plan-02-database-rls-complete` |
| 3 — Auth & role routing | SSR clients, middleware, sign-in/out/forgot/update-password, role layouts | `plan-03-auth-routing-complete` |
| 4a — Admin layout + Users CRUD + Invite/Onboarding | Admin shell (sidebar + user menu), `/admin/users` full CRUD (invite/edit/deactivate/hard-delete), `/onboarding`, migration 0003 | `plan-04a-admin-users-complete` |
| 4b — Properties CRUD | `/admin/properties` list/create/detail+edit, shared `PropertyForm`, per-field audit, soft-delete via Active toggle, curated US timezones, migration 0004 (RLS recursion fix) | `plan-04b-properties-crud-complete` |
| 4c — Assignments + call availability | Primary-agent assignment card on property detail (assign/reassign/unassign, close-then-insert, audited), per-property `accepting_calls` toggle on `/admin` overview (optimistic, not audited), `lib/assignments/` (`planAssignmentChange`, `validateAgentId`), migration 0005 (one-active partial unique index), seed gains OWNER + 2 AGENTs | `plan-04c-assignments-availability-complete` |
| 5 | Voice path + agent dashboard — **split into 5a (backend voice) + 5b (dashboard softphone)** during 2026-05-30 brainstorm | — |
| **5a** | **Backend voice path** — Twilio webhooks (`incoming`/`dial-result`/`status`), `lib/voice/` (routing + dedup + TwiML), `lib/twilio/` (HMAC verify), `twilio_identity` consolidation onto `lc_<uuid>`. Plan: `docs/plans/2026-05-30-05a-voice-backend.md` · Twilio setup: `docs/setup/2026-05-30-twilio-voice-setup.md` | `plan-05a-voice-backend-complete` (smoke-confirmed `t13-smoke-confirmed`) |
| 5b | Agent/admin softphone (shared widget, both portals) + `/api/twilio/token` + presence (heartbeat + `AWAY` status + OFFLINE cron) + answer route (`handled_by`/`answered_at`/`IN_PROGRESS`). Spec: `docs/specs/2026-05-31-05b-agent-softphone-design.md` | spec written, plan next |
| 6a | Kiosk app + agent video split-screen (Agora), migration 0007 | `plan-06a-kiosk-video-complete` |
| 6b | Playbook — signed-URL route + PDF viewer in the agent overlay | `plan-06b-playbook-complete` |
| **6c** | **Emergency call** — real 911 via a Twilio Conference (guest + agent + 911) merged through the existing `<Dial action>` seam; PSAP routing via the number's registered E911 address; `incidents` table; agent in-call mute/leave driven server-side via the Conference Participant API (a redirected Client leg is SDK-uncontrollable); verified end-to-end with the **933** test number. Migrations 0008–0009. Spec: `docs/specs/2026-06-02-06c-emergency-call-design.md`, plan: `docs/plans/2026-06-02-06c-emergency-call.md` | `plan-06c-emergency-complete` |
| 7 | Owner portal *(reordered after kiosk — call views show AUDIO+VIDEO from day one)* — **split into 7a (read views) + 7b (self-service writes)** during 2026-06-02 brainstorm | — |
| **7a** | **Owner portal read views** — mobile-first shell (header + bottom tab bar) + Home glance cards (agent presence, today-count, open-incident badge) + property detail (read; routing DID hidden) + call history (filter + load-more) + call detail (+ dark recording seam) + incident list/detail (read). `lib/owner/` helpers TDD'd. `<AutoRefresh>` poller (router.refresh on 20s + focus). Zero migrations / new routes / service-role. Spec: `docs/specs/2026-06-02-07a-owner-portal-design.md` · Plan: `docs/plans/2026-06-02-07a-owner-portal.md` | `plan-07a-owner-portal-complete` |
| **7b** | **Owner self-service writes** — kiosk-content inline editing (8 fields, inline Edit/Save/Cancel card) + playbook upload/view (service-role route, versioned key, 1h signed URL) + incident resolve (optional note, final, column-guard trigger). Migration 0010: `incidents.resolution_note` column + owner UPDATE RLS on `properties`/`incidents` + two BEFORE UPDATE column-guard triggers (`updated_at` excluded from property guard). 231 tests passing. Spec: `docs/specs/2026-06-03-07b-owner-writes-design.md` · Plan: `docs/plans/2026-06-03-07b-owner-writes.md` | `plan-07b-owner-writes-complete` |
| **8** | **Observability** — Sentry (both apps, PII-scrubbed `beforeSend`) + `@vercel/analytics` + admin `/audit` viewer (filter + load-more, 2-query actor merge) + admin `/status` health page driven by a generic `health_signals` heartbeat registry (Twilio webhook + cron self-report) and live Supabase/Sentry probes. Migration 0011. 252 tests passing. Spec: `docs/specs/2026-06-03-08-observability-design.md` · Plan: `docs/plans/2026-06-03-08-observability.md` | `plan-08-observability-complete` |
| **9** | **Email-free admin provisioning** (unblocks pilot without SMTP) — admin creates users with a typed temp password (`provisionUser` → `admin.createUser({email_confirm:true})`, no email sent); `profiles.must_change_password` forces first-login password change via the `requireRole` gate → `/onboarding` (cleared on finish); admin-driven password reset; specific sign-in error states + deactivated-user block; reusable `PasswordInput` show/hide on all password forms; email invite/reset paths dormant (re-enable seam = `/auth/confirm` + email-templates doc). Migration 0012 also closes a **profiles self-update privilege-escalation hole** with a column-guard trigger (only `full_name` is self-editable; verified via simulated-JWT SQL). 257 tests. Spec: `docs/specs/2026-06-04-09-admin-provisioning-design.md` · Plan: `docs/plans/2026-06-04-09-admin-provisioning.md` | `plan-09-admin-provisioning-complete` |
| **Audit fix** | **2026-06-06 readiness-audit remediation** — 10 PRs merged to `main`: 911 trigger atomicity + dispatch-failure + REST timeouts, 911 re-join retry, OWNER-reject on live-video routes + ringing-query time-bound, audit-after-delete, call-finalization correctness (status webhook + reaper + cron fail-closed + kiosk one-call), presence ON_CALL preservation, voice-boot hardening, low-risk cleanup, admin column cleanup. Migrations **0013** (FK indexes + search_path pin) + **0014** (helper EXECUTE revoke from PUBLIC) applied to prod. Triage BUG-bucket + cheap freebies; DEFER-V2/intentional items left. Audit: `docs/audits/2026-06-06-readiness-audit.md` (+ `…-triage.md`) | merged to `main` |
| **UI/UX Stage 0** | **Design direction LOCKED** — brand thesis (human reached through a screen + seam motif), palette (navy/coral/mint + cool neutrals), type (Solitude/Outfit/JetBrains Mono/Vonique 43), shape/shadow/motion/voice, per-surface art direction. Spec: `docs/specs/2026-06-07-ui-ux-stage0-design-direction.md` | merged |
| **UI/UX Stage 1 — Foundation** | **Brand token layer** (Tailwind v4 `@theme`, portal + kiosk mirror), **self-hosted fonts** (next/font portal + `@font-face` kiosk; real Solitude + Vonique 43, Envato license confirmed), **re-skinned shadcn primitives** + new `Card` (token layer, not forked; `accent`/`live` variants, brand focus ring), shared **`Wordmark`/`LogoMark`** with the seam hairline (logo = home), removed Jazz Club `--kiosk-*` leftovers. Plan: `docs/plans/2026-06-07-ui-ux-stage1-foundation.md` | PR [#13](https://github.com/kthakkar1983/lobby-connect/pull/13) merged |
| **UI/UX Stage 2 — Kiosk** | **Kiosk repaint (surface 1 of 3)** — every guest screen rebranded: Home (concierge split 55/45), recording notice (X-to-close + coral Continue), Ringing/Connected (seam **ring→frame** motif, shared `CallControls`, coral End/Cancel — no red), Apology (apology-only copy, visible countdown), repainted Loading + Reconnecting. New `--color-call` deep-navy video token, `CLOSE_DISCLOSURE` transition, removed deferred `Connected`/`Ringing` hardcoded hex. **Owner-selectable Home style** `kiosk_cta_style` (`warm`/`accent`/`classic`, default `warm`) end-to-end: migration **0015** (column + owner column-guard whitelist) + owner-portal picker (same Edit/Save txn, audited) + config API + kiosk rendering. Big Home line = static "Good evening" (later made time-aware in the owner-portal surface). Spec: `docs/specs/2026-06-07-stage2-kiosk-repaint-design.md` · Plan: `docs/plans/2026-06-07-stage2-kiosk-repaint.md` | PR #14 merged to `main` |
| **UI/UX Stage 2 — Owner portal** | **Owner portal repaint (surface 2 of 3)** — token/composition layer only (no route/data/RLS/API/migration changes). New shared `greetingForHour` (time-aware greeting, also applied to kiosk Home); new owner components `StatTile`/`StatusPill`(+pure `lib/owner/status-pill.ts`)/`SectionCard`/`CallRow`/`IncidentRow`; `lib/owner` helpers (`isLivePresence`, `formatTimeOnly`, `dayGroupLabel`, `latestCallTime`, brand-token presence dots; dropped dead `*BadgeVariant`). Screens: shell (seam hairline + coral active nav), Home (rich property cards: greeting + presence + Calls-today/Open/Last-call stats + mint live-edge / red open-incident edge), Calls list (day-grouped card rows), Call detail, Incidents list, Incident detail (status-colored header), Property detail (identity header + SectionCards + presence), kiosk-content/playbook SectionCard chrome, on-brand skeletons. Brand semantics: incidents/911=`destructive` red, coral=`accent` only, mint=`live`. Subagent-driven (17 tasks, per-task spec+quality review + final whole-branch review). Spec: `docs/specs/2026-06-07-stage2-owner-portal-repaint-design.md` · Plan: `docs/plans/2026-06-07-stage2-owner-portal-repaint.md` | PR [#15](https://github.com/kthakkar1983/lobby-connect/pull/15) merged to `main` |
| **UI/UX Stage 2 — Agent/Admin** | **Agent/Admin repaint (surface 3 of 3) — DONE, merged + prod-deployed.** Repaint + *light read-only* dashboards (no migrations/new routes/call-logic changes). New `lib/dashboard/` pure helpers (`countToday`/`avgPickupSeconds`/`sumTodayDurationSeconds`, `countOnlineAgents`, `lineStatusFromPhase` + context) TDD'd; new `components/dashboard/` (GreetingLine, LineBeacon, LineStatusProvider). Agent dashboard (serif greeting + mint/red **line beacon**, Today/Avg-pickup/**Talk-time** stats, recent calls, glowing softphone seam-ring, coverage rail) + UserMenu/seam header. Admin **operations board** (Agents-online/Calls-today/Open-incidents/Accepting stat strip + properties ops table w/ inline Covering toggle; **no Kiosk column** — heartbeat is a no-op). Softphone repaint (mint Accept, **coral Hang up**, relocated **solid-red 911** + reworded confirm dialog + notify forward-compat seam, beacon reporting via context). Video overlay (seam header + hotel name, deep-navy `--color-call` stage, seam PiP, playbook skeleton, **coral End**, greyed Hold/Swap — no red in video). Tables: zebra audit + filled status pills; status-card dots → mint/**coral=degraded**/red (no amber token); SectionCard property-detail; loading skeletons; coral active nav. Brand rule: **red = 911/destructive only**. Subagent-driven (12 tasks, per-task spec+quality review + final whole-branch review). 347 tests green. Spec: `docs/specs/2026-06-08-stage2-agent-admin-repaint-design.md` · Plan: `docs/plans/2026-06-08-stage2-agent-admin-repaint.md` | merged `a0f7cf6` (tag `plan-stage2-agent-admin-complete`), prod `dpl_74ACE5…` READY |
| **UI/UX Stage 3 — States/motion/a11y/copy** | **Final UI/UX phase — DONE, merged.** Logic-orthogonal polish (no migrations/routes/RLS/call-logic). **Motion:** `--ease-*`/`--duration-*` tokens (both apps), the Stage 0 **seam drift** (`@property --seam-angle`, active-call surfaces only), brand `Skeleton` shimmer, `Button` off `transition-all`, **universal `prefers-reduced-motion` net**. **States:** new `EmptyState`+`ErrorState` primitives wired into all 9 zero-item sites, on-brand `global-error` + segment `error.tsx` (agent/admin/owner), kiosk first-load/reconnecting `aria-live`. **A11y:** formal **WCAG 2.1 AA audit** (`docs/audits/2026-06-08-wcag-2.1-aa-audit.md`) → remediation — contrast (`muted-foreground→#5E6E85`, `live-foreground→#048765`, new `--color-accent-text #BE4B2F`, mint Accept→navy text), **D1** keep coral `#E05A39` + CTA labels at WCAG large-text, **D2** `--color-input→#919598` (form controls only), skip-links + `<main id>` (2.4.1), `sr-only` h1s (2.4.6). **Copy:** light shared `lib/copy` (+ kiosk mirror), sign-in errors migrated (strings unchanged), Stage 0 voice pass; 911 confirm stays inline. 347 tests green; whole-branch review acted on. Spec: `docs/specs/2026-06-08-stage3-states-motion-a11y-copy-design.md` · Plan: `docs/plans/2026-06-08-stage3-states-motion-a11y-copy.md` | merged `21906d0` (PR [#16](https://github.com/kthakkar1983/lobby-connect/pull/16), tag `plan-stage3-states-motion-a11y-copy-complete`) |
| **UI/UX — COMPLETE** | All four UI/UX phases shipped (Stage 1 foundation + 3 Stage 2 repaints + Stage 3 polish). **Open follow-ups (non-blocking):** live in-browser visual / screen-reader / reduced-motion pass (fold into smoke — Stage 3 contrast is math-verified, not eyeballed); kiosk CTA-picker `radiogroup` upgrade; the **Solitude capital-W** font fix (crossed-W reads as "V"; no OpenType alternate — decide swap-font vs accept). | — |
| **Notes durability + error surfacing + owner Calls tab** *(audit-remediation interlude, between Phase 1 and P2-1)* | **`reliableFetch`** (`lib/http/reliable-fetch.ts`) — retry network/5xx + Sentry on exhaustion (`Response\|null`; callers treat `null \|\| !res.ok` as failure). Call-notes save **decoupled from call phase**: a hard failure preserves the typed Room#/notes and surfaces a Retry/Discard affordance (softphone phase-independent banner + `notesSave`/`pendingNotes`; video-call keeps the overlay mounted, `finalizingRef` guards only teardown) — never silently dropped. `answered` / `emergency-control` (incl. **observable, self-reverting live-911 mute**) / `end-video` routed through the helper; emergency **trigger** stays bespoke (not auto-retried — life-safety) + Sentry; **20s presence heartbeat best-effort by design**. Owner **Calls tab**: note icon + **inline accordion expand** (shared `CallDetailBody` reused by the kept incident-deep-linked detail page) + enriched list query/batched incidents map + **Audio/Video filter**; property-detail "Recent calls" shares `CallRow` so it expands inline too (handler/incident resolved for parity). **Zero migrations / RLS / new routes**; `POST /api/calls/notes` untouched. Subagent-driven (per-task spec+quality review + opus whole-branch final). 360 tests. Spec/plan: `docs/specs/2026-06-10-notes-and-errors-design.md` · `docs/plans/2026-06-10-notes-and-errors.md` | PR [#17](https://github.com/kthakkar1983/lobby-connect/pull/17) merged to `main` |
| **Phase 2 — Security/tenancy seam extractions** *(2026-06-10 architecture audit, P2-1…P2-5)* | **`requireApiActor()` + `fetchOperatorCall()`** (`lib/auth/api-actor.ts`) replace the `getUser→profile→operator→role` preamble across **all 12 session API routes** — the **v2 multi-tenancy seam** (gates role+active+operator only; no per-property scoping). Also `parseVerifiedTwilioWebhook()`+`APOLOGY_MESSAGE`/`twimlResponse` (`lib/twilio/client.ts`+`lib/voice/twiml.ts`, 3 HMAC webhooks); `claimCall()`/`finalizeCallPayload()`/`ACTIVE_CALL_STATES` (`lib/voice/call-state.ts`)+`computeDurationSeconds()` (`lib/calls/duration.ts`); PII scrubber+kiosk↔portal DTOs→`@lc/shared`; `diffFields()`/`emptyToNull()` (`lib/audit/diff.ts`). **Two behavior fixes (A1/H3):** deactivated users (`active=false`) → **403 on all API routes** (one gate in `requireApiActor`); `/api/twilio/voice/answered` **rejects OWNER + claims via `claimCall`** (loser→409, winner-only `ON_CALL` — mirrors H3 onto audio); agent `calls/[id]/playbook` rejects OWNER. **911 path byte-identical** (opus-verified line-by-line); **zero DB/RLS/migration changes**; assignment-scoping deferred to v2. Subagent-driven (per-task spec+code review, opus on 911 + opus whole-branch final → GO). 411 tests (395 portal + 16 shared). **Prod smoke confirmed (2026-06-12).** Spec/plan: `docs/specs/2026-06-11-phase2-seam-extractions-design.md` · `docs/plans/2026-06-11-phase2-seam-extractions.md` | PR [#18](https://github.com/kthakkar1983/lobby-connect/pull/18) merged to `main` (`7c553e8`) |
| **Phase 3 — Perf/caching/parallelization** *(2026-06-10 architecture audit, P3-1…P3-6)* | React `cache()` session resolution (`lib/auth/session.ts`) + `cache()` agent-coverage (`lib/auth/agent-coverage.ts`) dedupe layout+page reads; `requireRole` widened (+`full_name`/`email`) so all **3 role layouts** drop their 2nd profiles read. Owner home `Promise.all` + per-property tz **count**/last-call queries; admin overview **count queries** (`{count,head:true}` — no row-ship, no silent 1000-cap; shared `lib/calls/today-window.ts` `startOfTodayUtc`). `unstable_cache` Sentry probe (60s, `/admin/status`). Owner Calls → **keyset cursor pages** (`lib/owner/calls-cursor.ts`, `(created_at,id)` `.or()`; **the one user-visible change** — accumulate→pages, ← Newest / Older →). **Voice incoming-webhook 8→4 hops + detached heartbeat** (`void recordHeartbeat().catch()` + `Promise.all` of 3 independent reads; opus, **byte-identical** across all 5 routing cases). **Zero migrations.** Subagent-driven (per-task spec+quality review, opus whole-branch + opus voice = GO). 412 tests. Spec/plan: `docs/specs/2026-06-12-phase3-perf-parallelization-design.md` · `docs/plans/2026-06-12-phase3-perf-parallelization.md` | merged `37ff689` (`--no-ff`) → prod `dpl_FNU5…` READY; **prod smoke CONFIRMED 2026-06-13**; tag `plan-phase3-perf-parallelization-complete` |
| **Playbook on audio + unified in-call overlay** *(post-Phase-3 feature, 2026-06-13)* | Surfaces the property **playbook on AUDIO calls** and **unifies the audio in-call screen** across agent + admin via one shared full-screen `AudioCallOverlay` rendered by the shared `Softphone` on `phase==="in-call"` (mirrors the video overlay; **~25% call-info rail / ~75% playbook**). Why it existed: the softphone (5b) shipped before the playbook (6b), which was built into the video overlay that audio calls don't render. `PlaybookPanel` moved to `components/call/` + gained a `basis` width prop (video unchanged at `basis-3/5`/60%); existing `GET /api/calls/[id]/playbook` reused as-is. **All call/notes/emergency state + handlers stay in `Softphone`** → the 6c emergency-conference control routing + the `pendingNotes` notes-durability banner are preserved; **video overlay rendered-output unchanged** (import-path only); `CallShell` shared-chrome left as a noted seam in both files. **Zero migrations / new routes / RLS.** Subagent-driven (per-task spec+quality review, opus whole-branch = GO). 414 tests. Spec/plan: `docs/specs/2026-06-13-audio-incall-overlay-playbook-design.md` · `docs/plans/2026-06-13-audio-incall-overlay-playbook.md` | merged `--no-ff` `edef163` → prod; **prod voice smoke PASS 2026-06-13**; minor UI tweaks deferred to the page-by-page UI/UX polish pass |
| **Phase 4 — Invariants, indexes & CI** *(final 2026-06-10 architecture-audit tranche, 2026-06-14)* | Encode the remaining audit invariants + stand up the project's first CI. **Single-sources:** `@lc/shared/protocol.ts` (all cross-app timing constants — ring window, presence staleness, reaper cutoffs, cron cadence — + module-load guard reaper>ring) (M7/A8); single `CallState` re-exported from `@lc/shared` (M3); **generated-base DB types** (`packages/shared/src/database.generated.ts` via `supabase gen types`) + curated `MergeDeep` overlay in `supabase-types.ts` re-narrowing CHECK columns to unions, with `gen:types` + `gen:types:check` drift check (M6); typed `AuditEvent.details` (M8); audit-action constants `lib/audit/actions.ts` (D10); shared `lib/storage/playbook.ts` signed-URL helper (D9). **Correctness (tested):** time-bound ON_CALL presence inference (S3); kiosk one-active-call **DB partial-unique-index** + 23505→409 (S8, migration 0016); parallel-dial cap at 10 + Sentry warn (S2). **Cleanups:** password reset repointed at `/auth/confirm` + dead `/auth/callback` deleted (M4); dead browser supabase client deleted (A7); parallelized reaper/sweep cron writes (S7); `audit_logs(operator_id,action,created_at)` index (S11, migration 0017); removed 22 `as never` route casts (→`as Route`) + `scripts/check-routes.mjs` guard so `typedRoutes` catches dead links (M2). **CI:** `.github/workflows/ci.yml` (lint/typecheck/test/route-guard/types-drift via local Supabase; CLI pinned `2.101.0`). 911 path byte-identical except one value-identical constant swap. A1/A2/A4 left as-is per triage. Subagent-driven (per-task spec+quality review + opus whole-branch = GO). 470 tests. Spec/plan: `docs/specs/2026-06-14-phase4-invariants-ci-design.md` · `docs/plans/2026-06-14-phase4-invariants-ci.md` | PR [#19](https://github.com/kthakkar1983/lobby-connect/pull/19) merged (`d17a146`); migrations 0016/0017 applied to prod; **CI green**; tag `plan-phase4-invariants-ci-complete` |
| **Brand revision — Foundation** *(2026-06-14 design → 2026-06-15 impl)* | System-wide rebrand: coral retired → **navy/teal/mint/blaze** + Raleway/Outfit/JetBrains + a real logo. Design LOCKED in `docs/brand/brand-guidelines.md`; impeccable context = `docs/PRODUCT.md` + `docs/DESIGN.md`. **Foundation built via `impeccable`:** color-token swap (portal `globals.css` + kiosk `index.css` mirror; new `--color-accent`=teal / `--color-attention`=blaze / `--color-ink`; coral's jobs split 3-way across every call site — missed/degraded/pending→blaze, links/nav/hover→teal, primary actions→mint, end/hang-up→navy); three fonts wired (Raleway via `next/font` + kiosk `@font-face`; Atelier/Radon deleted); shared `LogoMark`/`Wordmark` (`components/brand/wordmark.tsx`) render **SVGO**-optimised assets (mark 106KB→0.66KB, wordmark 109KB→2.1KB; `apps/portal/svgo.config.mjs` + `pnpm -F @lc/portal optimize:svg`); `Button` default→mint (+ navy `neutral`, teal `accent`); `Badge` `attention`; middleware fix (serve `/brand/*` statics). 428 tests + portal/kiosk builds green; `/sign-in` render verified. Zero migrations/routes/RLS. **NEXT: Layout phase** (brand doc §5 — unify agent/admin shells, real depth/hierarchy; dashboards first; Kumar brings per-page notes). | **on branch `brand-revision`** — committed, NOT merged to `main`, NOT deployed |
| **Brand revision — Login layout** *(2026-06-15)* | First **Layout-phase** surface, via `impeccable`. Sign-in redesigned as a **split**: navy brand panel (drifting **connection-lines** + "front desk, after hours." headline + 3px vertical seam) beside an **elevated form card** (radius 16, two-layer `--shadow-xl` lift, **seam gradient across the top**, centered wordmark home-link `h-12`, divider). Shared `app/(auth)/layout.tsx` → inherited by sign-in / forgot-password / onboarding; mobile collapses to the card. New `components/brand/floating-paths.tsx` (efferd "Background Paths" reworked: brand `currentColor` teal+mint on navy, `useReducedMotion()` guard, deterministic durations, `aria-hidden`) on the new **`motion`** dep (1 pkg). New tokens `--gradient-seam-vertical` + `--shadow-xl`; field placeholders added; **copy = deferred final pass**. typecheck + lint + 420 portal tests green. Zero migrations/routes/RLS. **NEXT: dashboards (agent/admin) — the core layout work.** | **on branch `brand-revision`** — committed, NOT merged/deployed |
| **Brand revision — Unified agent/admin shell** *(2026-06-16)* | Second **Layout-phase** surface, via `impeccable`. One shared `apps/portal/components/app-shell.tsx` (role-param; both `(agent)`/`(admin)` layouts → thin auth-and-delegate wrappers). **Navy rail** (recoloured `--color-sidebar-*` tokens, **not** a shadcn fork) + a 2px **vertical seam** on the `SidebarInset` left edge. **Reversed logo** on the rail: `LogoMark`/`Wordmark` + new `LogoLockup` gained an `onDark` prop; new `mark-on-dark`/`wordmark-on-dark` SVGs + the partner's `mark+wordmark{,-on-dark}` lockup (SVGO'd 244KB→2.8KB, viewBox cropped to `12 336 990 376`). **Hover-expand** (220ms intent delay + keyboard focus; header toggle removed). Role-aware nav (`NavItem` `exact` prop fixes index-route active; teal-wash active, no side-stripe). **3-col** with the persistent 320px right call-rail (admin's horizontal softphone strip retired into it). **Account menu** = avatar-only trigger → a **"boarding pass"** dropdown (`components/account-menu.tsx`; connection-ring `.lc-avatar-halo` via token `color-mix`) — **agent/admin only; owner keeps its own `UserMenu`** (restored). 428 tests + typecheck + lint green; verified in-browser. **Rejected:** account menu in the rail footer (fought hover-expand). Zero migrations/routes/RLS. Spec: brand doc §5.2. **NEXT (new chat): dashboard CONTENT + softphone-overlap (§5.3).** | **on branch `brand-revision`** — committed, NOT merged/deployed |
| **Brand revision — Login error states** *(2026-06-16)* | Small Layout-phase polish on sign-in, via `impeccable`. **Field shake + red invalid outline on every failed attempt:** reusable `.lc-shake` keyframe in `globals.css` (decaying `translate3d`, 360ms `--ease-out`, replays per-submit via a class-toggle reset on `onAnimationEnd`; honored by the reduced-motion net — movement off, red border still reads); red via `aria-invalid` → the shared `Input`'s existing `border-destructive`. Email field **routed through the shared `Input`** (was hand-rolled) so both boxes share one vocabulary + get the red state. **Native HTML5 validation retired** (`<form noValidate>`) — it fired the browser bubble (warning icon) and **blocked submit before the action ran**, so empty/format errors never reached the shake/red/message path (only wrong-creds did); now every error takes one path (plain custom message + shake, no bubbles/icons). New tested `validateSignInInput` (`lib/auth/sign-in-errors.ts`, reuses now-exported `EMAIL_RE`); copy `auth.required` + `auth.invalidEmail` (label-consistent "email", not "user ID"); `invalidCredentials` unchanged. 423 portal tests; verified in-browser (empty/format/creds). Zero migrations/routes/RLS. **NEXT (new chat): dashboard CONTENT + softphone-overlap (§5.3).** | **on branch `brand-revision`** — committed, NOT merged/deployed |
| **Brand revision — Dashboards + shared header (Stage 5.3)** *(2026-06-16)* | Core Layout-phase surface, via `impeccable` (browser-verified, agent + admin). **Shared gradient `DashboardHeader`** — tall top-aligned band, **teal→navy** (left→right), static connection-lines, cream time-aware greeting, **light avatar + `.lc-avatar-halo`**, bottom seam; the taller top-aligned layout lets the boarding-pass dropdown open *into the header*, not onto a card. **Shell:** the 320px call-rail is removed → the softphone stays **mounted in the shared layout for BOTH roles** (the line persists on every route), shown as a card on the dashboard home and hidden (still mounted) + `IncomingCallToast` off-home (new `dashboard-workspace.tsx`); **all call/notes/911 logic untouched** (idle restyle only: Line-ready pill, seam ring, Accepting toggle). **Agent** bento: 4 stats (Answered/Missed/Avg-pickup/**Avg-call-length**), hourly phone/video chart + talk time, recent calls, "your pod" channel bars. **Admin** command center: pulse [Live calls/Agents online/Open incidents/**Phone health**], Tonight card + outcomes strip, properties board (presence + Covering + needs-attention), team-on-now, operator-wide recent. New TDD'd helpers (`lib/dashboard/calls.ts` + `phone-health.ts`); shared `channel-viz`/`DashTile`; bento `shadow-md` lift; rail hover-intent 220→450ms. **Decisions:** agent dashboard is **agent-scoped, not pod-wide** (RLS `calls_select` exposes only the agent's own `handled_by` calls; true pod-scope = a v2 RLS branch — admin IS operator-wide); **phone-health "path down" red = v2 seam** (`twilio_webhook` is info-mode, can't tell down from a quiet night). Zero migrations/routes/RLS. Spec: `docs/specs/2026-06-16-stage5.3-dashboards-shared-header-design.md`. **This merge ships the whole brand-revision layout phase (foundation + sign-in + shell + error-states + dashboards) to `main` → prod.** | **MERGED to `main`** (`7e1deb2`) → Vercel prod; live-testing while owner-portal + kiosk layout work continues |
| **Brand revision — Dashboards follow-up + Phone-health page** *(2026-06-17, post-Stage-5.3 prod polish, session 22)* | Five prod merges fixing/extending the just-shipped agent+admin dashboards from Kumar's live-testing. **New admin route `/admin/phone-health`** (clickable "Phone health" `DashTile`) — read-only per-property line status (mint OK / blaze needs-attention via `phoneHealthRollup`; routing DID + front-desk/after-hours sublines; FAILED-today reason from new TDD'd `failureSummaryToday`; **real Twilio failure reason = v2 seam** — needs a `calls.failure_reason` column from the status webhook). **Recent-calls** → expandable `RecentCallRow` (hotel-name-only title; **note icon as its own right-aligned column**; expand → started/duration/room/caller/handler + notes; admin resolves handler names; queries +`caller_number`/`notes`/`handled_by_user_id`; no incident link — agent/admin have no incident route, v2 seam). **Incoming-call placement** → video gets its own **persistent "Video" card under the softphone** (`VideoCallHost` moved into the aside; idle "Ready" → mint Accept); audio incoming stays in-card; off-home audio toast bottom-right. **Video reliability:** ringtone **autoplay priming** on first gesture; **audio-only camera fallback** — a busy webcam (NotReadableError) no longer drops the call (guest connects; regression test). **Hourly-volume chart** bars were collapsing to 0 (flex `items-end` → indefinite column height) → fixed with definite column height (`items-stretch`+`h-full`). **Deferred:** admin off-tab video nudge (`docs/v2-backlog.md`). Zero migrations/RLS/deps; 458 tests + `next build` + CI green. Merges `8460a5f`/`0ab2104`/`47aad68`/`29cbbbe`/`45d3a63`. | merged to `main` → prod `pbo0jgdxz…` READY |
| **Brand revision — Owner portal LAYOUT redesign** *(2026-06-17, session 23)* | Final brand-revision **portal** surface (kiosk remains). brainstorm(visual-companion)→spec→plan→**subagent-driven** (12 tasks, per-task spec+quality review + opus whole-branch = GO). **Adaptive owner Home (direction C):** 1 hotel → full single-hotel overview (`components/owner/property-overview.tsx`: coverage strip + drill-through `DashTile`s + tonight `HourlyVolumeChart`/quiet-night + recent/incidents/manage); N → rich cards (prior count-query grid preserved). Gradient `DashboardHeader` greeting **Home-only** (chrome **direction A**; owner keeps its `UserMenu` in the slim white bar; inner pages calm + seam hairline). **Metrics drill through** (Answered/Missed → `/owner/calls?outcome=…`). **New shared call layer:** `lib/calls/filters.ts` (`parseOutcome`/`statesForOutcome`/`buildCallsHref`, TDD) + promoted `components/call/{call-row,call-detail-body}` (incident link **injected** + blaze; owner passes link, admin none) + `components/call/call-filters.tsx`; orphaned owner `greeting.tsx` removed. **Owner Calls** + **outcome filter** (Answered/Missed/Failed) atop the channel filter. **Incidents → blaze** + factual red `911` tag (resolves brand §3.2 red-vs-blaze → blaze); **FAILED call pill → muted** (§7, missed stays blaze). **New admin route `/admin/calls`** (operator-wide; shared filters + Hotel filter + sidebar nav) + admin dashboard tiles/recent **deep-link** in (`StatTile` gained optional `href`). **Zero migrations/RLS/call-logic/service-role.** 465 tests + typecheck + lint + build + check:routes green. **Live browser pass on prod PENDING** (sandbox can't run dev). Spec/plan: `docs/specs/2026-06-17-owner-portal-redesign-design.md` · `docs/plans/2026-06-17-owner-portal-redesign.md` | merged `--no-ff` `c1638cc` → prod `lhquew8bs…` READY |
| **Brand revision — Kiosk redesign** *(2026-06-18, session 24)* | **Final brand-revision surface** (kiosk) — brand layout phase now COMPLETE. brainstorm(visual-companion)→spec→plan→**subagent-driven** (8 tasks, per-task review + opus whole-branch = GO). **Home = "tap anywhere to connect":** the whole screen is one `<button>`; 50/50 login-style split — animated **navy invitation** (new CSS `ConnectionLines` + pulsing connect beacon + the line) │ seam │ light **greeting + small "Good to know" card** (per-field; hotel **name only**, no logo per brand §2). **Recording-consent screen removed** (no recording in v1) → folded into Ringing as a quiet line; **`disclosure` state collapsed** out of the call-machine (`TAP_CALL`→ringing; new screen-guarded `CALL_STARTED`); **all Agora/call logic byte-identical** (opus line-verified). **Ringing** rebranded (connecting field + spinning seam ring + recording line); **self-view PiP top-right in every stage** (Connected PiP moved up); Apology restyled; **Loading drops the LC logo**. **Connection-lines = the same `motion`-based `FloatingPaths` as the login** — shipped first as a lean CSS stand-in, then (per live feedback) swapped to the sign-in component so they match, and slowed ~2x for a calmer big-screen feel (`motion` added to the kiosk). New kiosk tokens `--gradient-brand-panel`/`--gradient-call-stage` + `lc-beacon` keyframe. Owner **CTA-style picker hidden** (`kiosk_cta_style` column/action/API left dormant as the re-enable seam). **Zero migrations/RLS/new routes.** kiosk 24 + portal 465 tests + typecheck + lint + build + check:routes green. **Prod smoke CONFIRMED 2026-06-18.** **Post-smoke fixes** (3 merges `443bfc6`/`c62cd47`/`8cf85eb`): FloatingPaths match + slowdown; End/Cancel tear down locally then notify the server in the background; **abortable in-flight call setup** (a generation token bumped by `teardown()`) so a Cancel during a cold first call can't orphan a ringing call; and **the real Cancel bug** — the Ringing spinner/text overlay was `absolute inset-0 z-[1]` with no `pointer-events-none`, sitting over the controls so taps never reached Cancel and `onCancel` never fired (fixed: `pointer-events-none` on the overlay + `z-20` on `CallControls`; **jsdom can't catch CSS-stacking bugs — only real-browser smoke can**). **First-call ring latency = serverless cold-start**, not a bug (the incoming-video poll is already 3s) → Pro keep-warm deferral. Spec/plan: `docs/specs/2026-06-18-kiosk-redesign-design.md` · `docs/plans/2026-06-18-kiosk-redesign.md` | merged `bd15103` + 3 fix-merges → **prod smoke CONFIRMED** |
| **Brand polish — favicon + audio in-call overlay** *(2026-06-18, session 25)* | Closes the brand revision. brainstorm(visual-companion)→spec→plan→**subagent-driven** (3 build tasks + whole-branch verification; per-task spec+quality review + opus final = GO). **(1) Logo/wordmark refresh** — re-ran SVGO on Kumar's 6 re-exported marks (~240KB→<3KB; light/dark verified) + synced `wordmark.tsx` intrinsic dims to the new viewBoxes (771×970 / 938×395 / 965×350). **(2) Favicon** — `app/icon.svg` (navy tile + **reversed** mark) + `app/apple-icon.tsx` (`next/og` ImageResponse 180×180, base64 SVG data-URI) + navy `viewport.themeColor`; verified legible at 16/32/64px + on a dark tab. **(3) Audio in-call overlay redesign** (`components/softphone/audio-call-overlay.tsx`) — **911 → header top-right alone** (red, max-separated from Hang up); navy `--color-call` **call card** (small self-tracked duration · hotel name · mint presence pulse · **bold HOTEL LOCAL TIME**); Room#/Notes + Mute/Hang up de-crammed control bar; **press-Enter-to-save** notes (in-field ⏎→✓/spinner/alert icon + `sr-only` `role=status` live region; reuses `saveNotes`/`pendingNotes` durability backstop). **Hotel local time wired via the `answered` route** (now returns `{ timeZone }` from a `properties(timezone)` embedded select — **no voice/TwiML/dial change**; the overlay only mounts post-accept); softphone `acceptCall` captures it, `saveNotes` returns a bool, new `saveNotesNow`. **All call/Twilio/emergency logic byte-identical** (opus line-verified; **`video-call.tsx` untouched**). **Audio-only scope** (video = noted shared-`CallShell` seam). **Whole-branch gate caught + fixed** a real regression: `softphone.test.tsx` fetch mocks lacked `.json()` (the new `acceptCall` body read) → mocks updated to honor the `Response` contract. **Zero migrations / RLS / new routes / service-role.** Full suite + typecheck + lint + check:routes + build green. **Prod audio voice smoke PENDING** (hotel-local-time ticks · Enter-saves-a-note · Mute/Hang up/911-confirm — only exercisable on prod). Spec/plan: `docs/specs/2026-06-18-brand-polish-favicon-audio-incall-design.md` · `docs/plans/2026-06-18-brand-polish-favicon-audio-incall.md` | merged `--no-ff` `5c83840` → prod (`dpl_FSoA…`); smoke pending |
| **Call reliability — presence-gate the dial** *(2026-06-19, `systematic-debugging`)* | **Root cause of the pilot's intermittent "no one is available":** the Twilio account's **concurrent-call limit = 1** (console-confirmed; business-verification submitted to raise it, ~2 days) colliding with routing's **parallel `<Dial>` to all assigned + `accepting_calls` agents** — at limit 1 only **one** outbound leg is placed; the other two are rejected (Twilio **error 10004 "concurrency exceeded"**, logged on every call). *Which* identity wins the single slot is a race, and since **2 of 3 agents (Dilnoza, Tejas) drifted offline but were still dialed**, the slot usually landed on a dead identity → greeting, **no ring**, apology. **Diagnosis (evidence chain):** prod `calls` rows (NO_ANSWERs dying in 7–15s, not the 120s ring) → Twilio per-leg logs (all 3 legs `failed`/`no-answer` at **0s**) → Twilio Monitor 10004 on every call → console limit=1. **NOT a regression** — 10004 + fast-misses go back ~2 weeks (06-05); the rate rose only as test agents went offline (refuted an earlier "tab-backgrounding" guess via Kumar's A/B test). **Fix (TDD):** routing now dials only **reachable** agents — new pure `isReachableForDial(status,lastSeenAt,nowMs)` in `lib/voice/presence.ts` (= `effectivePresence(...)==="AVAILABLE"`, so a **stale heartbeat is unreachable even though the OFFLINE sweep is daily**); `resolvePrimaryAgent`/`resolveAvailableAdmins` select `status,last_seen_at` and gate on it; empty-targets now also emits a `Sentry` warning so a genuinely-uncovered call is observable. With one online agent the fan-out **collapses to a single leg → fits limit 1 → connects reliably**; the concurrency increase later re-enables multi-agent fan-out (same code). `planDial`/TwiML **byte-identical**; zero migrations/RLS/new routes; full suite + typecheck + lint + build + check:routes green. Tracker: `docs/v1-punchlist.md` §A. | merged `--no-ff` `2072105` → prod (`dpl_HhkT1Go…`); **single-agent voice smoke CONFIRMED 2026-06-22** (audio connects reliably); Twilio concurrency cap + multi-agent fan-out → **v2** (`docs/v2-backlog.md`) |
| **v1.0.1 — s1-test fixes** *(2026-06-22, session 30, `systematic-debugging`)* | Four issues from the s1 smoke. **(1) Video rang everyone:** `GET /api/calls/incoming-video` returned every RINGING video call to any agent/admin (audio already scoped); new `resolveTargetPropertyIds` scopes the poll to the audio dial set — assigned primary agent + admins `accepting_calls=true` (presence NOT gated; polling proves liveness). Prod proof: a video call was `handled_by` an ADMIN with covering off. **(2) Answered calls mislabeled NO_ANSWER:** the kiosk finalizer downgraded an IN_PROGRESS call to NO_ANSWER on a `cancelled`/`no-answer` reason (concurrent-accept race from #1, or guest ending a connected call) — new pure `resolveFinalState(reason,answered)` in `lib/voice/call-state.ts` enforces *answered ⇒ never NO_ANSWER*; **15 corrupted prod rows backfilled to COMPLETED** (back to 06-06). **(3) Phantom-ring tab unidentifiable:** new `lib/hooks/use-ringing-tab-title.ts` flashes the tab title while ringing (audio + video). **(4) Chart vs recent-calls colours:** shared `lib/dashboard/channel-colors.ts` (teal=phone/navy=video) used by chart + recent-call icon (outcome dots unchanged). TDD (+scoping, +`resolveFinalState`, +`useRingingTabTitle`); full suite + typecheck + lint + check:routes + build + CI green. Zero migrations/RLS/new routes. **Prod smoke CONFIRMED 2026-06-22.** | merged `--no-ff` `2aeba0d` (fix `0249159`) → prod; released **`v1.0.1`** |
| **v1.1 — Live captions (agent-side)** *(2026-06-25, session 31, brainstorm→spec→plan; NOT STARTED)* | Real-time on-screen captions of the **guest's** speech on the **agent's** in-call screen (video + phone), to help agents with heavy accents/regional dialects. **Agent-side only** (two-way kiosk captioning cut → documented future seam); **video ships first** (night volume skews kiosk + lower-risk Agora track access), phone second; both in v1.1. **Speechmatics** behind a one-module swap seam (`lib/captions/provider.ts`), accuracy validated **live** (no upfront bake-off — no stored pilot audio). **Ephemeral by construction — zero migrations / RLS / persistence.** Architecture = client-side: tap the guest's remote `MediaStreamTrack` (Agora `getMediaStreamTrack()` / Twilio `getRemoteStream()`) → Speechmatics realtime WS; one server route mints a short-lived JWT (`@speechmatics/auth`), key stays server-side. **BLOCKED on:** a Speechmatics account + `SPEECHMATICS_API_KEY` (Kumar provisioning before next session — code + unit tests build without it; needed for the env step + live smoke). Spec: `docs/specs/2026-06-25-v1.1-live-captions-design.md` · Plan (8 TDD tasks): `docs/plans/2026-06-25-v1.1-live-captions.md` · **Handoff (start here): `docs/handoffs/2026-06-25-v1.1-live-captions-handoff.md`** | branch `v1.1-live-captions` (spec `2c0810b` + plan `d44471a`); not merged/pushed/deployed |

**Key patterns established so far:**

- **Server Actions** live in `app/(admin)/admin/<section>/actions.ts` — thin wrappers around tested `lib/` helpers. Import `requireRole` first, validate, call helper, audit, `revalidatePath`.
- **API-route auth seam** (Phase 2): authenticated API routes resolve their actor via `requireApiActor({ allow: Role[] })` (`lib/auth/api-actor.ts`) — session→profile→**active**→role gate, returns `ApiActor | NextResponse` (early-return on `instanceof NextResponse`) — then `fetchOperatorCall<T>(actor, id, columns)` for operator-scoped call reads. **This is the v2 multi-tenant query-layer seam** (decision #6): the operator filter changes in one file. Route-specific authz (emergency handled-by, notes `handled_by` self-scope) stays in the route *after* the actor resolves. **Do not add per-property/assignment scoping** — deferred to v2. Twilio HMAC webhooks use `parseVerifiedTwilioWebhook()` (`lib/twilio/client.ts`), NOT the actor.
- **`lib/users/` TDD pattern**: pure helpers (`validate.ts`, `guards.ts`, `provision.ts`) unit-tested with Vitest mocks before wiring into Server Actions. (`provision.ts` replaced the old email-invite `invite.ts` in Plan 9.)
- **Auth is admin-provisioned, not email-invited** (Plan 9): users are created with a typed temp password via `admin.auth.admin.createUser({ email_confirm: true })` (no email/SMTP); `profiles.must_change_password` forces first-login password change through the `requireRole` gate. Email invite/reset remain dormant for when SMTP lands.
- **Column-level write guards on `profiles`** (Plan 9, migration 0012): RLS is row-level only, so `profiles_update_self` let a non-admin PATCH any of their own columns (role/active escalation). A `BEFORE UPDATE` trigger (`enforce_profile_self_columns`, same pattern as 0010's `enforce_owner_*`) restricts a non-admin self-update to `full_name`. Guard keys on `auth.uid() IS NOT NULL AND auth.uid()=NEW.id AND current_user_role() <> 'ADMIN'`, so service-role/admin writes skip it. **All `profiles.status`/`last_seen_at` writes therefore MUST stay service-role** (presence routes + cron already do).
- **Admin pages**: Server Component `page.tsx` fetches data (via user-scoped client), passes to a `"use client"` `*-table.tsx` that owns all dialogs/sheets.
- **Typed routes** (updated Phase 4/M2): `typedRoutes: true` in `next.config.ts`. **Do NOT use `as never`** — it's banned by `scripts/check-routes.mjs` (run via `pnpm check:routes`, gated in CI). Static-literal hrefs need no cast; dynamic/interpolated hrefs (template literals, `router.push`) use `as Route` (`import type { Route } from "next"`); generic nav components type their `href` prop as `Route`. A genuinely not-yet-built route may keep a cast only if annotated `// FORWARD-REF:`.
- **Cross-app timing constants** (Phase 4/M7/A8): the ring window, presence staleness (`PRESENCE_STALE_AFTER_MS`), reaper cutoffs (`REAP_*`), and cron cadence (`CRON_SWEEP_INTERVAL_MS`) live ONLY in `packages/shared/src/protocol.ts` (imported by both portal + kiosk). A module-load guard enforces reaper-window > ring-window. Don't re-declare these anywhere.
- **DB types are generated + overlaid** (Phase 4/M6): `packages/shared/src/database.generated.ts` is machine-generated — **never hand-edit it**. Regenerate via `pnpm gen:types` (needs `supabase start` running + Supabase **CLI 2.101.0**, the pinned version; newer CLIs require an access token even for `--local`). `supabase-types.ts` is the curated `MergeDeep` overlay that re-narrows CHECK columns (`state`/`role`/`status`/etc.) to string unions and re-exports every name. After any migration, run `pnpm gen:types` and commit; CI's `pnpm gen:types:check` fails on drift.
- **CI exists** (Phase 4): `.github/workflows/ci.yml` runs `pnpm lint`/`typecheck`/`test`/`check:routes`/`gen:types:check` on every push-to-main + PR (local Supabase via Docker; no prod secrets). Keep all of these green. **Note:** per-package `lint` scopes to `app/components/lib` — only the root `eslint .` (in `pnpm lint`/CI) lints `tests/`.
- **Self-edit guards**: `lib/users/guards.ts` — `assertNotSelfDemote`, `assertNotSelfDeactivate`, `assertNotSelfDelete`. Always call server-side in actions; mirror in UI (disable controls, hide menu items).
- **Hard delete**: always requires typed email confirm in the AlertDialog. Audit row written BEFORE the delete call.
- **Migrations**: `supabase/migrations/0003_audit_actor_set_null.sql` sets `audit_logs.actor_user_id` FK to ON DELETE SET NULL so hard-deletes don't fail on prior audit rows.
- **RLS cross-table checks**: a policy on table A that must check table B (and vice-versa) causes "infinite recursion detected in policy." Put the existence check in a `SECURITY DEFINER` SQL function with `set search_path = public` (like `current_user_operator_id()`) — it runs as the owner and does not re-enter B's RLS. See `0004_fix_rls_recursion.sql` (`user_owns_property`, `user_is_assigned_to_property`).
- **Temporal-row invariant (assignments)**: "active" = the row with `effective_until IS NULL`. The one-active-per-property rule is guarded by a partial unique index (`0005`, `property_assignments_one_active`), and the Server Action mutates with **close-then-insert** (stamp the prior row's `effective_until`, then insert the new one) so a mid-failure leaves the property unassigned (safe), never double-assigned. A concurrent double-assign hits the index → caught as `23505` → friendly retry message. Pure decision logic lives in `lib/assignments/plan.ts` (`planAssignmentChange`).
- **Optimistic boolean toggle**: roll back to `!next` (not a closed-over `prev`) so a rapid double-toggle can't restore a stale value. See `availability-cards.tsx`.
- **RSC client boundary (Next 15.5)**: passing a non-serializable prop — a function, or a lucide icon *component* — from a Server Component to a Client Component is now a fatal 500 ("Functions cannot be passed directly to Client Components"). It was silently tolerated in 15.1.x. A component that hands icon components to a client child must itself be `"use client"`, or pass an already-rendered `<Icon />` element instead of the component.
- **Video-call finalization is multi-owner + idempotent** (session 6, post-Plan-9 fix): a VIDEO `calls` row is finalized by whichever of three paths fires first — the kiosk (`/api/kiosk/call-ended`), the agent (`/api/calls/[id]/end-video`, on Agora `user-left` or the End button), or the reaper cron (`/api/cron/reap-stale-calls`, the both-sides-gone backstop). Each update is **state-guarded** (`.in("state",["RINGING","IN_PROGRESS"])` / `.eq("state","IN_PROGRESS")`) so the first writer wins and late writers no-op — no leaked `IN_PROGRESS` rows, no clobbering a COMPLETED row back to FAILED. AUDIO never needs this (Twilio status webhooks finalize it server-side). The original bug: finalization was kiosk-only, so a kiosk crash leaked the row forever, invisible to DB/audit/Sentry. Kiosk also gained Agora `connection-state-change` handling (reconnect overlay → apology) + an `ErrorBoundary`; the portal gained `app/global-error.tsx`. Pure logic in `lib/calls/reaper.ts` + `apps/kiosk/src/lib/connection.ts`.
- **Vercel Hobby crons must be daily-granularity** (`m h * * *`), max 2 — a sub-daily schedule (`*/15 * * * *`) **ERRORS the deploy**, it does not silently cap. The portal runs 2 daily crons (presence 08:00 + reaper 20:00 UTC). Tighten the reaper to `*/15` only after moving to Vercel Pro. (Details in `memory/build-quirks.md`.)

## Reading order at session start

1. This file (CLAUDE.md)
2. `MEMORY.md` → `project-status.md` for current plan and next steps
3. The relevant plan in `docs/plans/` for the task at hand
