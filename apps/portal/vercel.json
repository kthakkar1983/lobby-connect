{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "cd ../.. && pnpm --filter @lc/portal build",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "outputDirectory": ".next",
  "crons": [
    { "path": "/api/cron/mark-stale-offline", "schedule": "* * * * *" }
  ]
}
